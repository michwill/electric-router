networks.py
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/
.cache/
*.npz
out/*.json
.DS_Store

# curve_solver writes its 16 MB snapshot into the cwd when run from here
snapshot_cache/
rust/target/
dist/

# Built by scripts/build_wasm.sh, 1.4 MB of it, and rebuilt whenever the
# crate changes -- so it is a build product like any other, not a source file.
# `flet-curve-demo/tools/build_router.py` runs that script rather than
# expecting a checkout to carry the binary.
rust/wasm/pkg/
